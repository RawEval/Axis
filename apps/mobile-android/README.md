# Axis Android

Kotlin + Jetpack Compose. Minimum Android 12. Material You. Phase 2 deliverable.

## Setup

```bash
# Requires Android Studio Hedgehog+ and JDK 17
./gradlew :app:installDebug
```

Shared business logic lives in `packages/kmm-shared`.
